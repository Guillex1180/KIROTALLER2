# Copy this to .env.local for local dev (usually not needed — proxy handles it)
# Set this in Netlify → Site configuration → Environment variables for production
VITE_API_URL=https://YOUR-APP-NAME.onrender.com
