[build]
  command   = "npm run build"
  publish   = "dist"

# SPA redirect: any unmatched path → index.html (required for React Router)
[[redirects]]
  from   = "/*"
  to     = "/index.html"
  status = 200
